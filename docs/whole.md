# Nanofield — Whole-Project State
*Last updated: September 17, 2026. Companion docs: `nanofield_ecommerce_plan.md` (business/catalog plan), `../AGENTS.md` (commands + conventions).*

## Objective
Standalone brand store for an Indonesia-based semiconductor / electronic-component and appliance-spare-part retailer (ex-Shopee/TikTok "Toko Sanjaya"), relaunched as **Nanofield**. B2C first; B2B held in reserve via Medusa's dormant B2B modules.

## Layout
- `apps/backend` (`@nanofield/backend`) — Medusa v2 app. API at `http://localhost:9000`, admin at `/app`. Node 20+, PostgreSQL 15+.
- `apps/storefront` — Next.js storefront. `http://localhost:8000`, default region `id` (Indonesia/IDR — imported prices only exist in IDR, so `/dk/` shows blank prices).
- Package manager for this install is **npm** (`package-lock.json` at root). Never introduce a second lockfile. Install deps inside the app that needs them (`cd apps/backend` / `cd apps/storefront`).
- Turborepo at root: `npm run dev | build | start | lint | test`. Dev runs as the user's own `npm run dev` (`@nanofield/storefront:dev` + `@nanofield/backend:dev`, turbo); the manual `Start-Process` era is over.

## Storefront — what is built
- **Hero**: one unified schematic scene (`HeroSchematic`, `modules/home/components/hero/index.tsx`) — power block, MCU, ADC/sensor, motor driver, comms, relay output; orthogonal nets with junction dots; inline diode/Zener/LED/MOSFET/NPN/relay/switch symbols. Eleven glowing signal pulses travel the power, bus, motor-loop, comms, and relay nets (`hero-pulse` / `-2` / `-3` overlays, `pathLength=100` dash-flow; hidden under `prefers-reduced-motion`); via dots are vivid green (`#00e676` with matching glow) and keep their staggered blink. Draggable overlay symbols are a separate layer (`DraggableSymbol`, positions persist to localStorage). CSS classes `hero-tr` / `hero-via` / `hero-pulse*` / `hero-spin-slow` / `hero-via-blink` live in `styles/globals.css`. Known pre-existing lint note: `<a href="/store/">` in hero — leave it.
- **Catalog** (`modules/store`): sort, grid/list view toggle, category checkbox filter with "All products" reset. Fixed 80/page (10 rows × 8-col grid) — a per-page selector was built then removed Sep 9 by decision (speed/simplicity over choice). Sep 12 filters: Option checkboxes (native ids), Spec checkboxes grouped by axis (`?spec=axis:value`), and a Has-datasheet toggle (`?has_datasheet=1`), all URL-shareable with round-trip; facet values come from `GET /store/facets` (scoped by category); spec/datasheet filtering runs in-memory inside the existing bulk fetch so counts stay exact; empty state names the blocking filter. Hero pulses use dedicated `--hero-pulse` vars (vivid cyan light `#06b6d4`, aqua dark `#5eead4`) — one notch above traces and brand teal in both themes. Schematic layer stays `opacity-30` in light mode (tried 60/45/40 Sep 8 — all too strong; original subtlety wins) — dark stays 40. Pulse visibility in light mode comes from the `--hero-pulse` color, not layer opacity. Filter state lives in the `?category=` URL param (`store/page.tsx` parses it). Reset pushes the bare path + `router.refresh()`; the grid remounts via a filter-signature `key` on the `<Suspense>` boundary; empty results render an explicit "No products match" panel with a "Show all products" link.
- **Search** (Sep 8 — storefront UI + one backend route, zero admin UI, no Meilisearch; extended Sep 12): `GET /store/search?q=` (backend `api/store/search`, Knex `.raw` via `__pg_connection__` — `manager` is NOT in route scope; Knex binds positionally so reused `$n` must expand to repeated `?`) ranks exact SKU/part_number/**mpn**/option-value/handle → title substring → **spec-metadata/category/family tier** → trigram similarity → tsvector rank, typo-tolerant (verified `ne55`→NE555), returns ordered ids; storefront hydrates via `listProducts` id filter and re-sorts to rank (`searchProductIds`, `?q=` in `store/page.tsx`, `SearchField` on hero above ENTER CATALOG + full-width catalog header with count + clear (compact placeholder "Search part number"). Navbar `SiteSearch` icon (left of hamburger, 18px, `gap-x-6`): compact overlay (`max-w-lg`, no shortcuts block — stock Medusa has none) with live product suggestions (title/handle from extended `/store/search` items) + static pages (Home/Store/Cart/Checkout/Account/Orders/Addresses/Profile + FAQ/Contact/Returns & Exchanges with matching Hugeicons, added Sep 11; Notifications page added Sep 17) + see-all-results. Needs `pg_trgm` extension (installed on dev DB; repeat for prod). SQL lives in template literals — LIKE escapes must be written doubled (`spec\\_%` in source) or JS collapses them.
- Sep 9: modal icons per page (Hugeicons), results scrollbar hidden (`no-scrollbar`), preview reports the detected Berat header (green badge) or NOT FOUND (red badge — variants default 500g). Weight parse verified live for ID formats; 0/1001 variants currently have weight, so the sheet's Berat column is unconfirmed.
- Shopee weights (Sep 9): `shipininfo.xlsx` (Informasi Pengiriman) carries `Berat Produk/g` per variation (verified: 1003 weights, 1–2000g, keys overlap imported `shopee_variation_id`s). Engine: `parseShip` + 4th `ship` upload (preview/execute/UI/multer) → `weightGrams` per variant (variation-id match, single-row pid fallback, sales-file detection last). Preview weight badge prefers the ship header. Re-import with the ship file attached to land real weights.
- shadcn set completed Sep 9 (39 components in `src/components/ui`; sheet/command/alert-dialog/calendar/toggle-group/pagination skipped - CLI overwrite prompts stall headless runs, install on demand). Existing customs (button/input/...) untouched - always answer "no" to overwrite. CLI files carry the project's pre-existing React-types tsc noise; runtime unaffected. Navbar search rebuilt on `Dialog` (portal + scroll-lock + persistent backdrop). Apply shadcn primitives to new UI first, migrate old custom UI opportunistically.
- **Catalog caching**: products/categories fetches use `force-cache` + `revalidate: 300` (5-min ISR). After deleting products/categories in the admin, **restart the storefront dev server once** to flush stale entries immediately.
- **Product pages**: transparent default product image with `size` prop (`xs|sm|md|lg|card`); gallery carousel with arrows/dots/counter; shared datasheet util (`lib/util/product-datasheet.ts`: `no_datasheet` opt-out → direct `datasheet_url` (any category) → `is_semiconductor=false` hides → `mpn`/`datasheet_search`/`part_number` search fallback; title is never used so hand tools/merch get no button) + `DatasheetButton` under the image ("View datasheet" direct vs "Find datasheet" search). Tabs card renamed "Datasheets & Documents": distributor stock/pricing links (DigiKey/Mouser/LCSC) REMOVED Sep 15 — direct-from-China sourcing makes them competitor ads that 0-result on obsolete Asian parts; the no-documents empty state counts the search fallback and links to `/contact`; Compliance card hides while empty (never auto-filled — wrong RoHS claims are a liability); cross-reference badges kept. Fallback URL is `view.jsp?Searchword=` (capital S) — the lowercase `search.jsp` format 404s live.
- **Cart**: optimistic `QuantityStepper` on cart page + cart dropdown; hover preview is viewport-clamped and hidden on mobile.
- **Home extras**: `NewArrivals` grid is 2-col mobile / 6×2 desktop (12 newest); side menu uses the navbar's exact scrolled frost recipe (theme-aware `color-mix`, was hardcoded navy that broke light mode); theme toggle sun icon goes `dark:text-foreground` (white like other nav icons); `images.qualities: [50, 75]` in `next.config.js` (Next 16 requirement).
- **Product specs honesty**: `SpecificationsTab` stock status derives from real `inventory_quantity` (any purchasable variant → In Stock, else Out of Stock; explicit metadata still wins) — was a blind "In Stock" default contradicting the buy button.
- Fonts Outfit (body) + Manrope (headings); teal primary `#0E7C8C`.
- **Support pages** (Sep 11): `/contact` (WhatsApp chat / email / visit cards), `/returns` (4-step RMA explainer + channel cards + Order-ID closing card), `/faq` (15 Q&As across orders/payments/shipping/returns/parts; renamed from `/customer-service` with a permanent redirect). Shared `PageHeader` (eyebrow + title + pretitle) used on all three AND on cart (moved out of the card), checkout (new), orders, order details, profile, addresses, account overview. Single contact source `@lib/store-contact` (WA `6285121550532` → display `0851-2155-0532`, `customer-support@nanofield.com`) — footer refactored onto it + new Support column (FAQ/Contact/Returns). Order-detail `Help` links wired (`/contact`, `/returns`) via `InteractiveLink`. `ContactCta` banner (Contact-us title + pretitle + WhatsApp button) mounted in `(main)/layout` (hidden on `/store` + the three support pages) and in `(checkout)/layout`. Search modal lists FAQ/Contact/Returns with matching Hugeicons (`HelpCircleIcon`, `CustomerService01Icon`, `ReturnRequestIcon`).
- **Error screens** (Sep 11): shared `ErrorScreen` (`modules/common/components/error-screen`) — centered Nanofield wordmark (nav classes), big code, title, cause message, frontpage link + optional retry. `PageBackdrop` gained opt-in `swarm` (floating schematic symbols on `animate-hero-drift`; grid-only default unchanged for account/cart/store). Applied to all four `not-found` routes + new `(main)/error.tsx` (live `error.message` + Try again) + `(main)/forbidden.tsx` (403) + root `global-error.tsx` (inline-styled last resort). No 503 page — nothing can trigger one yet.
- **Orders page pagination** (Sep 11): 5-at-a-time, newest first, with a "Show more orders" button (`?limit=` growth, fetch-one-extra `hasMore` trick — no lib changes). Previously: latest 10 with older orders unreachable.
- **Order identity + tracking QR** (Sep 11): order cards/details lead with the raw Order ID (transfer reference) + small display number underneath; per-product QRs removed (one tracking QR per order). QR also renders on the admin widget + printed label, pointing at the storefront order URL.

## Backend — what is built
- **Admin branding** (`src/admin/lib/brand-dom.ts` + `widgets/branding.tsx` + `widgets/login-branding.tsx`): login circuit backdrop (22-trace board, 14 glyph types, 39+ spinning floaters, dash-flow signal pulses on all 21 via-terminated traces, vivid blinking vias), topbar badge, sidebar avatar blobs, favicon, Outfit+Manrope, static-copy scrubber (hides Docs/Changelog, rewrites Medusa strings; re-runs on click/focusin/popstate), settings-sidebar section spacing (inline-margin marking, same graceful-degradation tradeoff).
- **Admin routes**: `/app/overview` (Nanofield home, rank 1), `/app/ecomm-import` (E-comm Import, rank 2, `ArrowUpTray` icon; `@medusajs/icons@2.19.0` added), `/app/rajaongkir-services` (Shipping Services, `TruckFast` icon — service catalog UI, see shipping bullets), `/app/announcements` (Announcements, `BellAlert` icon — bell broadcasts + banner/strip/image management with previews, toasts, edit/delete), `/app/sender-profile` (Store Sender, `BuildingStorefront` icon — label Pengirim block, env fallback).
- **Product datasheet widget** on `product.details.side` (toggle + URL + MPN). Datasheet UI rule (storefront `getDatasheetInfo`, shared by button + tabs): `no_datasheet` opt-out → direct `datasheet_url` (any product) → search fallback ONLY when `is_semiconductor` is explicitly true plus an identifier; title never used. Saving an MPN into an empty `part_number` also fills it (with hint), so search and datasheet never disagree; manual values always win over importer-derived ones. The importer sets `part_number` + `is_semiconductor` from the SKU prefix (`classifySku`: IC/TRS/MOS/DIO/CAP/RES/IND/XTL/CRY/LED → true, everything else — incl. FUS appliance spares, MOD, tools — false), and on update prefers an operator-set `mpn` as `part_number`. The same prefix rule auto-flags `is_semiconductor` on product create/update via the `product-classify` subscriber (guarded — writes only on difference, so no loops; silent; variant SKUs preferred, slugified handle as fallback). Manual create forms have no widget zone, so the subscriber — not UI injection — is the create-path mechanism.
- **Shopee importer** (Shopee-only by decision): thin CLI (`scripts/import-shopee.mjs`) calling Admin API routes (`src/api/admin/shopee-imports/`: `preview`, `execute`, `jobs`, `jobs/[id]`), shared engine (`engine.ts`), in-memory job store, multer middleware (`src/api/middlewares.ts`, 8MB). Preview + report UIs list skipped rows (pid/key + reason) and errors. Catalog-consistency contract (full spec in `docs/catalog-consistency-phases.md`): variation values normalized at plan time (`normalizeOptionValue`, case preserved) with a preview badge; categories resolve through the 29-entry canonical allowlist (`category-map.ts`, `*Others` kept) with raw path kept as `category_path` metadata; variant labels derive `spec_family`/`spec_*`/monotonic `has_datasheet` (`specs.ts`, strict anchored matching, family-gated); re-imports add zero option values on identical files. Backfill script (`scripts/backfill-catalog-consistency.ts`, dry-run default, `--apply` on explicit confirmation) brings the live catalog onto the same contract without touching prices/stock.
- **Admin bell** (Notification Center, `feed` channel via the local provider in `medusa-config.ts` + `notifyFeed` helper + `formatIDR` for `Rp 190.000` amounts). Consistent rule: important completions AND failures notify with title + one line; rows persist forever server-side (paginated drawer, unread = newer than your last open, refetch 60s — no expiry, no server delete). `feed-companion` topbar widget toasts arrivals newer than the last visit (60s poll, max 3, backlog never floods). Posters: Shopee import done/failed, bulk stock/delete done/failed (route-level too), `order.placed` + `order.canceled`, shipment pipeline (`order.fulfillment_created` packed, `shipment.created` shipped, AWB label added, `order.fulfillment_canceled`, auto-delivery POD via tracking-sync), `user.created/updated/deleted` (team security), `product-category.created/updated/deleted`. Deliberately silent: `product.*` (import volume — summaries cover it, including the classify subscriber), `order.updated` (routine touches). Returns ARE covered since Sep 17 (`return-activity` subscriber: requested/received/claim/exchange, admin + customer copy). Manual AWB entry remains — the delivered note posts itself once the courier confirms POD. CORRECTION Sep 11: the DOM sweep that auto-hid read rows + injected ×/toggle buttons (`sweepFeedDrawer`) is REVERTED and deleted — it froze the whole admin when the bell drawer opened (mutation churn against core React markup). Drawer is stock Medusa behavior again; toasts untouched. Do not re-add drawer DOM manipulation.
- **Bulk stock & delete** lives on the Products list itself (`product-bulk-tools` widget, `product.list.before` zone): a compact two-button bar opens separate Set-stock / Delete drawers (Medusa UI `Drawer`), each with debounced search (50/page + Load more through all results), checkbox select ("All on screen" + "Select all N matching" up to 3000 across pages), preview, and execute; stock uses per-product quantities (global qty seeds each row at preview, rows stay editable); delete additionally confirms via `usePrompt`. The two buttons teleport next to the table's Create/Export button, else the Add-filter row (inline bar below the table as fallback); a persistent observer re-attaches after core re-renders, since Medusa exposes no toolbar zone. API errors surface with HTTP status + server message via toast and inline text. Backed by `src/api/admin/bulk-products/` (`preview` resolves SKUs to products/stock, `execute` set-stocks at the first stock location or permanently deletes — both 8-way parallel with a 5000-row cap, full counts never silently truncated); results also bell-notified. Drawer uses a single scroll container (no nested scroll traps); preview display capped at 200 rows.
- **Order analytics** on `/app/overview`: daily (30d) / monthly (12m) / yearly revenue + order-count charts (recharts — backend dep) with revenue/orders/avg stat cards, most-common currency formatting, and an empty state before the first sale.
- **Shipping label / print receipt** (Sep 11, thermal-first): `GET /admin/orders/[id]/receipt` returns a print-ready HTML label — Penerima (customer shipping address) full-width on top, Pengirim (store sender) + tracking QR side-by-side below, no prices. `template.ts` holds the markup (Manrope/Outfit via Google Fonts, `!important` + `document.fonts.ready` gating because Chrome print preview otherwise drops webfonts; QR via `api.qrserver.com` at 500px source with wide quiet zone, half-row width so thermal heads render it scannably; print CSS in physical `pt`/`mm` so 80mm rolls print true-size — print dialog must use margins None + background graphics on, normal/dark density). `order-receipt.tsx` widget (`order.details.side.before`) shows a Print button + QR preview ONLY when `payment_status` is captured/paid, and waits for fonts + images before calling `window.print()`. `query.graph` does NOT return computed `payment_status`, so the route does not gate on it (widget already does). Sender address from env (`STORE_NAME/PHONE/ADDRESS_1/CITY/PROVINCE/COUNTRY_CODE`, defaults Nanofield Pasar Jambi `+62 851-2155-0532`) — no admin UI for these yet (offered, not built).
- **Auto-delivery sync** (Sep 12): `src/jobs/tracking-sync.ts` (cron `TRACKING_SYNC_CRON`, default every 6h) polls Komerce `POST track/waybill` for shipped-but-undelivered fulfillments carrying an AWB and runs core `markOrderFulfillmentAsDeliveredWorkflow` when the courier confirms POD — the storefront card flips to Delivered on its own, plus a bell note. Tracker client lives in `rajaongkir-fulfillment/tracking.ts` (same `key:` conventions, tolerant parse; JNE gets the receiver phone's last 5 digits). Quota discipline (shares 100 hits/day with checkout quotes): oldest-first, `TRACKING_SYNC_MAX_PER_RUN=5`, skips shipments younger than `TRACKING_SYNC_MIN_AGE_HOURS=6`, unknown couriers skipped, per-shipment try/catch — a run never throws. Entering the AWB stays manual; everything after is automatic.
- **Customer email** (Sep 16): exactly one email provider is ever active — Resend in production (`RESEND_API_KEY` + single-sender-verified `RESEND_FROM`), Mailtrap sandbox in dev (`MAILTRAP_HOST/PORT/USER/PASS` SMTP credentials + `MAILTRAP_FROM`), selected by conditional registration in `medusa-config.ts`; neither set → customer sends skip silently. Shared table-HTML templates (`resend-notification/templates.ts`: confirmation / shipped with big AWB + tracking QR / delivered / canceled, each with order timeline + plain-text twin) feed both providers. `notifyCustomer` helper (never throws, skips without recipient) fires from `order.placed`/`order.canceled`, `shipment.created`, and tracking-sync delivery. The admin "notify customer" checkbox works only once an email provider is active — before that it errors into the void.
- **Customer bell** (Sep 16, Phase A): logged-in storefront navbar bell fed by the same `feed` channel as the admin bell, keyed by lowercased customer email (`GET /store/notifications`, customer-authenticated, own + broadcast rows newest-first). Writers post alongside the emails at order confirmed/canceled (`order-placed`), shipped (`shipment-activity`), delivered (`tracking-sync`), each with `orderId` for deep-linking to `/account/orders/details/[id]`. Client (`notification-bell`) polls every 60s, unread = newer than per-customer localStorage seen key. Returns feed (`subscribers/return-activity.ts`: `order.return_requested` / `return_received` / `claim_created` / `exchange_created`, each with admin + customer copy and `orderId` link) done same day; no refund event exists in this Medusa version so the received copy promises the refund. Return emails built Sep 17 (`nanofield-return-requested` / `-received` / `-update` templates + `notifyCustomer` wiring in `return-activity`; Mailtrap dev, Resend prod). Customer bell feature complete: orders + returns + announcements, panel + page, 60s poll. Navbar order account → cart → bell → theme with uniform gap-x-6 (no ml-4), bell Popover full-height centered with focus:outline-none (no stuck ring). Store route serves own-email rows + `broadcast:true` announcements only — admin operational notes ("Open Orders to fulfill", "Mark as shipped…") never leak to customers; legacy dotted-decimal totals normalized to Rp at read time. Panel keeps its list on failed polls (ok flag, no blanking), per-row X dismiss in panel + history (panel header has no close button by decision — the bell icon toggles), and the history page is linked from the desktop + mobile account sidebar and the search modal index.
- **Storefront banners** (Sep 16): `banner` module (Banner model: type/title/description/link/image_url/starts_at/ends_at/is_published, migration via `db:generate`) + `GET /store/banners` (live only: published + within dates) + admin `GET/POST /admin/banners` and `PATCH /admin/banners/:id` (publish toggle, no delete by decision). Announcement broadcasts can twin as homepage/store strips via "banner until"; image banners upload JPEG/PNG/WebP ≤5MB through the file-local provider into the same `/static` volume (relative path persisted, storefront prefixes backend URL). Past broadcasts support in-place Edit (PATCH, applies going forward only — already-seen copies keep per-device read state) and DELETE (bell record only, confirm-gated; banner twins are separate rows). Each banner row shows a live preview (image thumb or strip mock). Every mutation toasts success (broadcast / strip / image / publish toggle / edit / delete); the admin bell keeps announcing new broadcasts via the existing feed-companion toasts. Strips can also be created standalone (no bell involved) for older broadcasts or one-off notes. Bell + strip both verified live after a full backend restart (an earlier wedged process served stale code with no listening port — kill stray node processes before restarting). Storefront `StoreBanners` (strip above image, stacked, `relative` so it paints over the PageBackdrop grid) on homepage (below hero) + store page; strip dismissible per-session, image fixed. Banner feed uncached (`no-store`) so publish/unpublish shows on the next visit; image URLs normalized to forward slashes (Windows `path.join` backslashes healed at read). Bell rows + history rows carry a per-row X dismiss sharing one persisted id set (`nf-cust-feed-dismissed`); server feed stays append-only. Side-menu panel text uses `text-ui-fg-base` so light mode stays readable. Phase B (history page `/account/notifications` with show-more + read marker, bell "View all" footer, login-gated side-menu entry) done same day; mobile entry carries no unread dot (desktop bell owns badge state). Phase D (admin Announcements page + `POST /admin/announcements` broadcast route, `to=""` + `broadcast:true` rows served by the existing store route, bell/history render an Announcement eyebrow + internal link) done same day; broadcasts also land in the admin bell so operators see what customers see; no targeting/scheduling/delete by decision.
- **RajaOngkir shipping** (Phase 1 code Sep 7, verified live Sep 8 — checkout verification still pending): custom fulfillment provider `src/modules/rajaongkir-fulfillment/` (`ModuleProvider(Modules.FULFILLMENT)`, `static identifier = "rajaongkir"`), injected into the fulfillment module's `options.providers` in `medusa-config.ts` (TOP-LEVEL `modules[]` entry crashes config load — `transformModules` requires `.service`; learned the hard way Sep 7) with `RAJAONGKIR_*` env (full set in `.env.template`). Targets the **V2 Komerce API** (`rajaongkir.komerce.id/api/v1`, `key:` header, SHIPPING COST key — not payment/QRIS). `calculatePrice`: origin subdistrict pinned `RAJAONGKIR_ORIGIN_ID=19363` (Pasar Jambi, Kota Jambi 36133) + destination via `domestic-destination` search (24h-cached), ONE `domestic-cost` call for all couriers split client-side by `code`, JNE REG exact + J&T cheapest (`jnt-eco` → EZ), summed variant weights. Live-verified Sep 8 (key works on V2 host; Jambi→Menteng 1kg = JNE REG 26000 + J&T EZ 22000). OKE/YES deliberately NOT offered: JNE publishes only REG (+JTR cargo) on Sumatera routes, so express labels would silently quote REG speed; NEVER throws — any failure returns flat `RAJAONGKIR_FALLBACK_AMOUNT` (Rp 20.000) so checkout can't block. `createFulfillment` stamps `{ courier, service, manual_booking: true }` (AWB booked manually). Importer engine parses Shopee `Berat` by header name (`detectWeightCol`, <100 → kg→g) into `weightGrams` on create/new/synced variants (500 g/item fallback when absent).
- `.gitignore` ignores `*.xlsx` at root. Never commit/print `.env` secrets. Never hand-edit migrations that may have run — add new ones.

## Data status (Sep 9, 2026 — real import live, old Sep 6 notes below kept for the open data issues)
- Live catalog: ~1000 real Shopee-imported products (IDR prices, Shopee images) + semiconductor categories (ICs, Transistors, MOSFETs, Capacitors, Resistors, Diodes, Modules & Boards, merch leftovers?). Verify no `lt-XXXX` loadtest rows remain (see Next §3).
- European Warehouse: soft-deleted Sep 8, stays deleted. All inventory at Pasar Jambi. Channel links only to Pasar Jambi.
- Real import source: Shopee mass-update Excel exports at repo root. Open data issues before import: 1 SKU collision (`TRS-0051`), 6 price mismatches, 2 duplicate listings, platform-gap merge decision, demo-seed handle collisions (`TRS-0051`, `IC-0399/0401`) — see plan Section 6.
- Cleanup required before/with real import: remove loadtest + merch + demo-seed products, resolve collisions, switch to IDR pricing.
- Stock reality (Sep 8–9): 543 SKUs at 0 / 325 at 1-2 / 133 at 3-10 (Pasar Jambi). Suspect: engine used fixed indices (price 6, stock 9) + naive `Number()/parseInt()` — Indonesian thousand-dots ("15.000"→15) and template column shifts both silently zero out. Hardened Sep 8: `parseIdNumber` (dots/commas/Rp, verified live) + header-name detection for Harga/Stok (exact-then-fuzzy, index fallback) + stock shown in dry-run preview samples. PROVEN Sep 9 against `informasipenjualan.xlsx` (1001 rows): real columns are Harga=6, **Stok=8** — engine had read index 9 ("Min. Jumlah Pembelian"), so the 543 zeros/325 ones are min-buy values, not stock (true stocks ~100). Detection resolves Stok=8 exactly. File has NO Berat column (0/1001 weighted) — weights can't import until Shopee includes them; 500g fallback stands. Green light: preview (confirms Stok + weight-column badge), then execute with publish ON.
- Real catalog is live (Sep 9): ~1000 imported Shopee products with IDR prices serve the storefront (grid + product pages verified). Product images hotlink `cf.shopee.co.id` — works today, fragile long-term (see R2 item below).
- Import batch-shape fix (Sep 9): this Medusa version's `location-levels/batch` requires `inventory_item_id` + `location_id` on update entries (id alone 400s). Engine `syncStock` fixed + verified live — re-running execute now syncs stock correctly (previous run updated 880 products, failed 843 stock syncs).
- Publication follows stock (Sep 9, user rule): 63 published zero-stock products force-drafted via API; engine auto-manages going forward (create: draft when 0/unpublished-run; update: draft on 0, republish restocked drafts only on publish-enabled runs — manual unpublishes respected). Stock dots on cards (emerald/red after SKU, same truth as buy button); cart dropdown widened 320→420px with 220px title column.
- Quick-add 500 + universal OUT OF STOCK (Sep 8, resolved): European Warehouse was soft-deleted 09:47 during admin location cleanup, orphaning all 1001 levels (channel link died the same second). Fixed by moving levels to Pasar Jambi + linking channel locations + engine syncs to channel's first location. Lesson: never delete a location holding inventory — move levels first.
- Follow-on outage (Sep 9, resolved): every page 500d with digest 565003953 for browsers holding a cart cookie. Chain: my link-restore had re-attached the DELETED European Warehouse to the channel → `list-shipping-options-for-cart` crashed reading `fulfillment_sets` of undefined → storefront pages (which fetch shipping options for carts) all died; cookie-less probes stayed green, which hid it. Fixed by deleting that link — channel points only at live Pasar Jambi. European Warehouse stays deleted; do not re-link or restore it.

## Gotchas
- Catalog cache: 5-min ISR on products/categories. Product AND category create/update/delete auto-purge the storefront via the `revalidate-storefront` subscriber → `POST /api/catalog/revalidate` (literal event strings — the `ProductEvents.*` constants have a prefix mismatch and don't fire for categories). Requires `STOREFRONT_URL` + matching `REVALIDATE_SECRET` in both env files (configured Sep 6). Restarting the storefront dev server flushes everything.
- Integration tests need a live PostgreSQL. No destructive DB commands without explicit confirmation.
- Backend must satisfy `@medusajs/eslint-plugin` recommended config — never disable a `@medusajs/*` rule; fix the code.
- `apps/storefront` lint currently ignore-patterns `src` files; `tsc --noEmit` has pre-existing project-wide React-type (TS2786) noise — when verifying, filter output to the files you touched.
- VSCode `@tailwind`/`@apply` "unknown at rule" warnings are editor-only noise, silenced via `.vscode/settings.json`. The Tailwind build is unaffected.
- Stock starter bug (fixed Sep 8, storefront `account-info/index.tsx`): the collapsible editor panel used `overflow-visible` while collapsed, leaving closed forms invisible-but-clickable over rows below — clicks on one section's Edit toggled/submitted a neighbor's form (phantom "updated successfully" badges on the wrong section). Fix: `overflow-hidden`. Badges are per-form state and now attribute correctly.
- Storefront account UX (Sep 8): billing editor surfaces the real server error (`errorMessage` from action state, was generic); address actions translate 401/`Unauthorized` into "Session expired — please log out and log back in, then retry" (`friendlyCustomerError` in `lib/data/customer.ts`); phone input fixed to `type="tel"`.
- Storefront `tsc` TS2786 noise on Button/Badge JSX is pre-existing on HEAD (verified via stash) — filter tsc output to touched files.
- Admin DOM tweaks must carry styling inline with the mark: an inject-once `<style>` tag goes stale for the session when its selector changes (Sep 8 spacing bug — marks applied, no visual change, survived even a versioned id). `applySettingsSpacing` sets `marginTop` directly and clears it on unmark.
- RajaOngkir integration targets the **V2 Komerce API, not legacy v1** (`api.rajaongkir.com/starter`): V2 free tier covers 17 couriers incl. **JNE + J&T** at 100 hits/day, has subdistrict-level ids, and the account/key live in the Collaborator dashboard. Provider ships `jne-reg` + `jnt-eco` on with `jne-oke/yes/ctc/ctcyes/jtr` pre-seeded off (100 hits/day free quota). Legacy v1 host is unreachable from this dev machine anyway.
- RajaOngkir usage rules: destination data MAY be cached (provider caches search 24h); **cost quotes must be requested live** (no cost cache). Weight unit is grams. Needs the SHIPPING COST (Cek Ongkir) key — sandbox key + sandbox base URL for testing first, live key at go-live.
- RajaOngkir quote inputs: destination = the checkout delivery-step address (`cart.shipping_address.city` + `province` — typed fresh or picked from saved account addresses; `/account/addresses` needs NO changes). Origin = pinned `RAJAONGKIR_ORIGIN_ID`, NOT the admin stock location (its city text can't resolve to an exact kecamatan) — still fill the stock location address in correctly for records + pickup.
- Shipping services are admin-managed, no console needed (Sep 8, round-trip verified): `rajaongkir` module (`shipping_service` table: code/courier/service_code/label/cheapest_match/is_enabled) + `/admin/rajaongkir-services` API + `/app/rajaongkir-services` page (Shipping Services). Built-ins (`jne-reg`, `jnt-eco`) can only be disabled; customs can be added/deleted; the fulfillment-option dropdown and quoting read the merged list live. Shipping *option type* (Standard/Express) never collides with the *fulfillment option* (courier+service payload) — different layers; use Standard for REG/EZ, Express for future same-day/instant. Pickup stays a separate Fixed/free option on the manual provider (UI-only, no code).
- Shipping pre-seeds (Sep 8, verified live): `jne-oke`/`jne-yes` OFF (absent on tested lanes — enable only after a lane proves them), `jne-ctc`/`jne-ctcyes`/`jne-jtr` OFF (verified intra-Jambi: CTC 10rb, CTCYES 14rb, JTR 40rb at 1kg). Toggle path proven end to end (JTR on→quotes→off, state restored). Origin stays env-pinned (`RAJAONGKIR_ORIGIN_ID`) by user decision — no origin UI; revisit for a second warehouse.
- Backend must be running for the storefront to render at all — Sep 7 proved both failure modes: dead backend → storefront 500s everywhere (incl. a misleading "Objects are not valid as a React child"); wrong provider registration shape in `medusa-config.ts` → silent instant boot crash (no log output; run `develop` in foreground to see it).
- Wedged dev processes (Sep 17): a stuck backend holds :9000 with nothing listening — login `fetch failed`, broken `/static` images, empty bell, no red anywhere. Fix is a full kill first (`Get-Process node | Stop-Process -Force`), then restart; a plain Ctrl+C + restart can leave the stray in place.
- `medusa develop` applies pending module migrations on boot (proven Sep 17: the `banner` table landed on restart) — but a brand-new module ALSO needs the restart itself; the admin UI hot-reloads alone and will show new pages against an old API, which looks exactly like a backend bug.
- Shipping live (Sep 9, Phase 1 CLOSED): provider attached, set + zone exist, 3 calculated options created via Admin API (JNE REG, J&T Express, JNE City Courier `so_01M2297...`; create requires `prices: []` + `type_id` standard + `data` = fulfillment option, else 400). Verified live: Jambi→Menteng Jakarta JNE REG Rp 26.000 + J&T EZ Rp 22.000; intra-Jambi JNE CTC Rp 10.000. Quotes scale per-kg rounded up on summed variant weights — NULL weights fall back to 500g/item (all current variants are NULL: re-import with Berat to lower light-item quotes). Card over-stock errors reworded via `friendlyMessage` map in `lib/util/medusa-error.ts` ("Not enough stock available — please lower the quantity"). Checkout `AddressSelect` fixed (controlled `value ?? ""`, `bg-ui-bg-field` panel to match inputs in dark mode). Cart `QuantityStepper` caps at real variant stock (+ grays out) with reserved error line (no layout shift).
- IC-0394 holds real stock again (reset Sep 17 from 10 ARTIFICIAL units — set Sep 8 purely to unblock checkout testing — to the operator-confirmed 19).

## Next — remaining & planned (Sep 11 order)
1. **Checkout verification** (DONE Sep 10–11): live test orders placed and Midtrans payments captured from the dashboard (orders #4/#5) — quotes, capture, and thermal label print all exercised on real orders.
2. **Stock truth** (user): re-run import preview, compare sample price/stock with the Informasi Penjualan sheet; only then re-execute. Reset IC-0394 to real qty after.
3. **Leftover sweep + Phase 6 backfill (STILL PENDING — the big one)** (verify): confirm no `lt-XXXX` loadtest / merch / demo-seed products remain in the live catalog. Then run the backfill with `--apply` on explicit operator confirmation + audit re-run (closes `docs/catalog-consistency-phases.md` Phase 6). Related: the script deliberately SKIPS the 3623 loadtest-flagged rows — decide sweep-before-backfill so metadata is never written onto rows slated for deletion.
4. **Midtrans Phase 2** (mostly DONE Sep 10): `midtrans-payment` provider module (Snap `createTransaction`, status mapping capture/settlement→captured, signed webhook → session actions, challenge approval, refund/cancel/expire), storefront confirmed/transfer pages, live Sandbox captures verified. Remaining: production-key cutover procedure at go-live.
5. **Store sender-settings UI** (DONE Sep 17): `sender-profile` module (singleton row) + `GET/PATCH /admin/sender-profile` + `/app/sender-profile` page (Store Sender, `BuildingStorefront` icon). Receipt route prefers the saved row, falls back to `STORE_*` env — labels never break on a fresh DB.
6. **Manual pickup option** (DONE Sep 17): seed script step 12 creates the `pickup`-type fulfillment set + zone + free "Pickup in store" flat option on the manual provider (idempotent, dry-run safe). Storefront already split pickup out by set type — no UI work needed.
7. **Weight-gated options** (DONE Sep 17): checkout hides JNE JTR/cargo options below `NEXT_PUBLIC_CARGO_MIN_WEIGHT_G` (default 10kg, same 500g/item fallback as backend quotes) with an "unlocks above X kg" hint showing current cart weight. Merchandising only — the server still quotes if selected.
8. **Return emails** (DONE Sep 17 — see bell bullet).
9. **Second warehouse / white-label** (deferred): origin is env-pinned; per-location origins need provider + UI work when real.
10. **Hosting + PSE** (decided Sep 16): Railway backend (Docker) + Vercel storefront — see `DEPLOY.md`; PSE registration check with accountant/lawyer still open.
11. **B2B modules** (reserve): dormant until real B2B demand.
12. **Redis** (DONE Sep 16): dev runs on in-memory fallback; prod uses the Railway Redis plugin (`REDIS_URL`) — no separate install work.
13. **Meilisearch — DROPPED**: Postgres full-text + trigram search shipped instead; no new infra.
14. **Transfer-request bell** (open, small): `order.transfer_requested` events exist and the storefront has a transfer form, but no subscriber posts them — skipped in Phase C as ownership-transfer, not returns.
15. **Notification retention** (DONE Sep 17): `notification-retention` job (daily 03:00, `FEED_RETENTION_DAYS` default 90, 0 disables) deletes `feed` rows older than the cutoff in oldest-first batches (2000/run cap). Never throws.
16. **Bell realtime + server read-state + mobile dot** (deferred by decision): 60s poll, per-device localStorage seen/dismissed state, and a dot-less mobile side-menu entry are the accepted tradeoffs — revisit only with real complaints.
17. **Production deploy** (runbook ready): execute `DEPLOY.md` — Railway backend (Dockerfile + volume + plugins), Vercel storefront, then the go-live flips (Midtrans live keys + webhook, RajaOngkir live key, Resend domain, tracking-sync quota watch).
- Expired customer sessions 401 every authed write while pages still render — surfaced as "Session expired — log out/in" (see account UX). If a logged-in user reports a dead action, re-login is step one.
